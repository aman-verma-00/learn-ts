import Stats, { perimeterRectangle, areaRectangle } from "./lib/geometry";

const x:Stats = {
    length : 10,
    breadth : 20
}
console.log(x)

console.log(perimeterRectangle({length:5, breadth:2}));